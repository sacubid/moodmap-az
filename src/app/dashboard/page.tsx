export default function Dashboard() {
  return (
    <main style={{padding:24}}>
      <h1>Private dashboard</h1>
      <p>If you see this after signing in (on Azure), auth routing works.</p>
    </main>
  );
}
